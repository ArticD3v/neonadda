{%- comment -%}
  Renders the Neon Adda wordmark — outlined pink "NEON" + cyan script "adda",
  recreated from the client's mark. Purely visual: the wrapping anchor must
  provide its own accessible name (the spans are aria-hidden).
{%- endcomment -%}
<span class="header-logo__wordmark" aria-hidden="true">
  <span class="header-logo__wordmark-neon">NEON</span><span class="header-logo__wordmark-adda">adda</span>
</span>
