{% comment %}
  Font preloads from the theme's font_picker settings are disabled.
  Neon Adda loads its fonts from Google Fonts via snippets/na-fonts.liquid,
  and the font CSS variables are overridden in assets/neonadda.css.
  Keeping this snippet as a safe no-op avoids a hard Liquid error
  ("font_url can only be used with a font drop") if a font_picker value
  resolves as a plain string instead of a font drop.
{% endcomment %}
